using System;

namespace Giraffe
{
    class MainClass
    {
        public static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            Console.ReadLine();
            /*
            Console.ReadLine() gives us the ability to view the console that displays
            the program being run. This is more relevant if Visual Studio is being run 
            on Windows, since I didn't have this issue with my Mac. Regardless, this
            simple line of code is pretty helpful nonetheless.
            */
        }
    }
}
